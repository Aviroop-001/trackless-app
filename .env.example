# ── Database (Neon) ──
# 1. Create a free database at https://neon.tech
# 2. Copy the connection string and paste below
# Without this, the waitlist API logs to console instead of persisting
DATABASE_URL=

# ── Optional: Formspree fallback (client-side only) ──
# Only used if you want the form to submit directly from the browser
# instead of going through /api/waitlist
# VITE_FORMSPREE_ID=
